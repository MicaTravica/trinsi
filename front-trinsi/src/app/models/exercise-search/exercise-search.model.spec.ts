import { ExerciseSearch } from './exercise-search.model';

describe('ExerciseSearch', () => {
  it('should create an instance', () => {
    expect(new ExerciseSearch(null, null, null, null, null)).toBeTruthy();
  });
});
