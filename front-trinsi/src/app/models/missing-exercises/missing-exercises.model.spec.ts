import { MissingExercises } from './missing-exercises.model';

describe('MissingExercises', () => {
  it('should create an instance', () => {
    expect(new MissingExercises(null, null, null)).toBeTruthy();
  });
});
